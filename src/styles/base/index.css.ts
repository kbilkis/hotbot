// Import reset styles (this applies global styles)
import "./reset.css.ts";

// Import theme tokens to make them available
import "../theme/tokens.css.ts";

// Export the new theme system
export * from "../theme/index.css.ts";
